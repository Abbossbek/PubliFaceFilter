<Page x:Class="PubliFaceFilter.Pages.SaveDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:PubliFaceFilter.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SaveDetailsPage">
    <Grid>
        <Image x:Name="img" />
        <Button x:Name="btnClose"
                Content="X"
                FontSize="25"
                Width="50"
                Height="50"
                Background="#88ffffff"
                Visibility="Collapsed"
                Command="materialDesign:DialogHost.CloseDialogCommand" VerticalAlignment="Top" HorizontalAlignment="Right"/>
        <TextBlock x:Name="tbTimer" Text="5" FontSize="25" Width="50" Height="50" TextAlignment="Center" Background="#88ffffff" VerticalAlignment="Top" HorizontalAlignment="Right"/>
        <TextBlock x:Name="tbPath" Text="5" FontSize="25" Height="40" TextAlignment="Center" Background="#88ffffff" VerticalAlignment="Bottom" />
    </Grid>
</Page>
